# سجلّ التغييرات

يُولَّد آلياً من أوصاف الالتزامات. لا يُحرَّر يدوياً.

## 3.0.4 — 2026-07-29

### إصلاحات

- رفعُ تثبيت naf-auth إلى v3.3.0 (`c27d826`)
- الخروج بـPOST وحده — رابطٌ يُخرج صاحبَه تكفي صورةٌ لتشغيله (`06ed10b`)

## 3.0.3 — 2026-07-28

### إصلاحات

- مسار إشعار الخروج الخلفي — الخروج من المركز يُنهي الجلسة هنا (`7690168`)

## 3.0.2 — 2026-07-28

### إصلاحات

- ترقية naf-auth إلى v3.1.2، وإدراج العضو يحتمل دخولين متزامنين (`9f5c26a`)

## 3.0.1 — 2026-07-28

### إصلاحات

- ترقية naf-auth إلى v3.1.1 — الدخول يبلّغ المركز بالصلاحية (`e9466c7`)

## 3.0.0 — 2026-07-28

### تغييرات كاسرة

- «مستخدم (اطّلاع)» يقرأ ولا يكتب (`be265e1`)

### إصلاحات

- ملف القفل يشير إلى v3.1.0 كما يشير الوصف (`451d5d5`)
- تعذّر تبليغ المنح يقول ذلك، بعد أن كان صامتاً (`114fa1f`)
- زرّ الخروج يحمل المصطلح المسجَّل (`a14cb84`)
- الخروج يخرج بالمستخدم، والمركز يعرض صلاحيته في هذه المنصة (`4b17a72`)

## 2.0.0 — 2026-07-28

### تغييرات كاسرة

- تثبيت الربط على naf-auth@v3.0.0 وقيم المركز الحقيقية (`5700940`)

### إصلاحات

- سير النشر يقبل إعداداً مثبَّتاً ولا يكاثر مساحات KV (`f31326a`)
- شكل الردّ يتبع طبيعة الطلب، والتبليغ العكسي على توقيعه القائم (`e4dbcac`)

## 1.0.0 — 2026-07-27

### تغييرات كاسرة

- ربط المنصة بنظام الدخول الموحّد — التركيب الخلفي (`829e2b0`)

### ميزات

- صفحة الرفض وشاشة الأعضاء والصلاحيات (`049eee1`)

### إصلاحات

- اعتماد مفردات السجلّ لشاشة الأعضاء وصفحة الرفض (`77cdd21`)
- معالجة ما رفعه التقرير قبل الدمج (`600aa69`)
- نسخ استجابة الأصول قبل كتابة ترويسات الأمان (`141987b`)

## 0.2.0 — 2026-07-27

### ميزات

- مُبدِّل المظهر ثلاثي الحالات، ونصّ الحالات بالرموز القوية (`f5961f9`)
- طلب إضافة نظام غير موجود في قاعدة المعرفة (`1ab164c`)

### إصلاحات

- أزرار تعرض شفرة JSX بدل الأيقونة (`10584b1`)
- تعذّر فتح الشريط الجانبي على الشاشات الصغيرة (`5df0cc3`)

## 0.1.0 — 2026-07-26

### ميزات

- الترقية إلى v1.6.0 وإتمام تصنيف العمل القانوني (`e41d422`)
- تطبيق ما سُجّل في السجلّ من فجوات (`84fa688`)
- اعتماد علامة ناف المتجهة من السجلّ (`fc67dd5`)
- استبدال الإيموجي بأيقونات Lucide المسجَّلة (`5dca249`)
- إسناد المسافات والأبعاد إلى سلّم ناف (`a6be277`)
- إسناد الخطوط إلى سلّم ناف (`b74917c`)
- تثبيت Tailwind v4 وإحلال ثيم ناف محلّ النظام الموازي (`8c5f614`)

### إصلاحات

- توحيد النصوص على قاموس مصطلحات ناف (`82ff06f`)
- الاتجاه — خصائص منطقية وعزل اتجاهي وتنسيق موحّد (`d0c95c0`)
