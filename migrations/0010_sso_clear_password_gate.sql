-- الدخول الموحّد — إسقاط بوابة تغيير كلمة المرور
--
-- `must_change_password` من هجرة `0004`: المسؤول ينشئ الحساب بكلمة مرور
-- افتراضية، ويُطلب من صاحبه تغييرها عند أول دخول.
--
-- بعد الربط لم يبقَ للبوابة معنى، وصارت مصيدة:
--   ١) `App.tsx` يحبس من رايته مرفوعة في شاشة تغيير كلمة المرور
--   ٢) والشاشة تنادي `/auth/change-password` التي تتحقّق من كلمة المرور
--      الحالية — ولا كلمة مرور لعضوٍ بابُه المركز، فيبقى محبوساً بلا مخرج
--
-- والعمود لا يُحذف: الحذف يكسر `SELECT`ات قائمة في `routes/auth.ts` و
-- `routes/admin.ts`، والهجرات للأمام فقط. تُصفَّر قيمته وحدها.

UPDATE users SET must_change_password = 0 WHERE must_change_password <> 0;
